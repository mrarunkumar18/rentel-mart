# STEP 1 — Auth Screens + Layout Shell
**Day:** 1 | **Screens:** S-16 Register, S-17 Login, S-18 Forgot Password

## Objectives
1. Create root layout with fonts (Inter from Google Fonts), global CSS with Tailwind directives
2. Create `(auth)` route group layout — centered card layout, no sidebar
3. Build Register page: First Name, Last Name, Email, Phone, Password, Confirm Password fields with inline validation
4. Build Login page: Email, Password, Remember Me toggle, Forgot Password link
5. Build Forgot Password page: Email field + submit
6. Create shared Navbar component with Rentify branding (use primary color `#1886FF`)
7. Set up mock auth state in `/src/mocks/auth.ts`

## Key Color Usage
- Primary buttons: `bg-primary text-white hover:bg-secondary`
- Links: `text-primary hover:text-secondary`
- Input focus: `focus:ring-2 focus:ring-secondary`
- Page background: `bg-white`

## After Completion
Update PROGRESS.md → Step 1. Commit: `[STEP-1] - Auth screens (login, register, forgot password) + layout shell`
