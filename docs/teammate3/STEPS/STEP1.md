# STEP 1 — Admin Layout + Sidebar + Routing + Auth Guard
**Day 1:** Build admin layout shell with sidebar (10 modules), responsive, breadcrumbs, admin auth guard middleware (check admin_roles). Mock admin session. Route: `/(admin)/layout.tsx`.
Commit: `[STEP-1] - Admin layout shell, sidebar navigation, auth guard`
