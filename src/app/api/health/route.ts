// Placeholder for Phase 4 API routes
// API routes will be created during Phase 4b when connecting to live Supabase

export async function GET() {
  return Response.json({ status: "ok", message: "Rentify API is ready" });
}
