export default function EditListingPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  // T2 STEP-6: Build edit listing form here
  return <h1 className="text-2xl font-bold text-primary">Edit Listing</h1>;
}
