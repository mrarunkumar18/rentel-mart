export default function OrderDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  // T2 STEP-5: Build order detail page here
  return <h1 className="text-2xl font-bold text-primary">Order Detail</h1>;
}
