# /src/types — TypeScript Type Definitions

## Files (created by Teammate 1 in Step 4):
- `database.ts` — Complete type definitions for all Supabase tables

## Rules
- All types must match the Supabase schema exactly
- No `any` types — use `unknown` when truly unknown
- Include Insert types (omit auto-generated fields) and Update types (partial)
- Define all enum types for status fields
